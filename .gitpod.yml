tasks:
  - name: dependencies
    init: >
      yarn global add lerna &&
      yarn &&
      gp sync-done bundle
  - name: dev server
    init: gp sync-await bundle
    command: yarn docusaurus:start

ports:
  - port: 3000
    onOpen: open-preview
